import java.awt.*;


    public  class PlayerBullet extends Bullet{

        public PlayerBullet(double x, double y, int damage, double vx, double vy, Color color, int max_dist) {
            super(x, y, damage, vx, vy, color, max_dist);
        }
    }
